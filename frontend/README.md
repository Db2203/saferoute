# SafeRoute frontend

Next.js (App Router) + Tailwind + Leaflet. Talks to the FastAPI backend at `localhost:8000`.

## Develop

```
npm install
npm run dev
```

Open http://localhost:3000.

## Build

```
npm run build
```
